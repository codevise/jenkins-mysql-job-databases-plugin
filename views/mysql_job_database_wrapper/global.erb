<%
# TODO: These form fields are displayed in the global config. But
# there values are not persisted.
#
# f = taglib("/lib/form")
# f.section(:title => 'MySQL Job Databases') do
#   f.entry(:title => 'Jenkins Database User', :field => 'jenkins_mysql_user') do
#     f.textbox
#   end
#   f.entry(:title => 'Jenkins Database Password', :field => 'jenkins_mysql_password', :description => 'These credentials are used to create databases for jobs.') do
#     f.textbox
#   end
# end
%>