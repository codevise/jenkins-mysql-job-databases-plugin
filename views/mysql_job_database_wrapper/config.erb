<%
f = taglib("/lib/form")
f.entry(:title => 'Database name', :field => 'database', :description => 'Environment variables available in shell build steps: $MYSQL_DATABASE, $MYSQL_USER, $MYSQL_PASSWORD.') do
  f.textbox
end
%>
